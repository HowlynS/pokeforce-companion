# CLAUDE.md - PokeForce Companion

You are assisting with PokeForce Companion.

## Mandatory Source of Truth

Before making or suggesting changes, respect these documents:

1. PokeForce Companion Project Brief
2. PokeForce Companion Technical Brief
3. AI_RULES.md
4. Current milestone instructions from ChatGPT

## Current Phase

Milestone 9 - Admin Workspace & Game Version Management is in progress; Slice 9A (Game Version foundation) is complete (see MILESTONES.md and DECISIONS.md 2026-07-17):

- Database-backed `GameVersion` model (unique name, optional release date, `isCurrent`); at most one version is current, enforced by the service transactions in `src/lib/game-versions.ts`, serialized by a shared `pg_advisory_xact_lock`; the first version ever created becomes current automatically.
- Gameplay verification is relational: `verifiedAt` + `verifiedGameVersionId` (ON DELETE RESTRICT) on Item, Location, AcquisitionSource, Recipe, and Profession. Categories deliberately have no verification. A referenced Game Version cannot be deleted.
- Verification is against a selected Game Version: every verification form carries the shared picker (`GameVersionVerificationControls`, submitting `verifiedGameVersionId`), defaulting to the current version; historical versions are valid selections; the id is validated server-side (tampered ids fail); a blank selection falls back to the current version; `verifiedAt` always comes from the server clock.
- `CURRENT_GAME_BUILD_ID` is retired; the row marked `isCurrent` is the only source of truth. The migration converted the existing `verifiedBuildId` strings into GameVersion rows (name = exact legacy string, not current), preserving every `verifiedAt`.
- Game Version management and ALL verification information are admin-only: a secondary settings destination at `/admin/settings/game-versions` (linked from a restrained Settings section on the admin dashboard — never primary navigation), and public pages never render version or verification text.
- Terminology: the contributor-facing term is "Game Version" everywhere; never display "Build".

Slice 9B.1 (shared admin shell) is complete: the `/admin` layout renders `AdminShell` — a persistent left sidebar (`AdminNav`, exactly six primary destinations: Dashboard, Items, Recipes, Professions, Categories, Locations; active state via `aria-current`, pure mapping in `src/lib/admin/admin-nav.ts`) plus a content area — after the unchanged `requireAdminUser()` gate. Admin pages render only their own content (never the public `AppShell`); `AdminWorkspace` provides the structural header/record-list/main/aside slots later slices fill (only the dashboard composes it so far). Acquisition Sources stay contextual under their owning item; Game Versions stay out of primary navigation.

Slice 9B.2 (shared admin editor primitives) is complete: resource-agnostic presentational components in `src/components/admin/` — `EditorHeader`, `EditorTabs` (link-based, caller supplies `active`), `ContextPanel`, `ImagePanel` (structural wrapper; upload behavior stays in the existing controls), `VerificationPanel` (composes `GameVersionVerificationControls`; status classified by the pure `src/lib/admin/verification-status.ts`: unverified/current/outdated), `TimestampsPanel`, and sticky `EditorActions` (plain HTML form submission; delete is a link to the existing confirmation route). Editor chrome uses the purple admin accent (`--color-admin-accent`); the public design system stays yellow. Component tests render to static markup via `react-dom/server` (`.test.tsx`, Node-only, still no DOM library). NO resource page uses these yet — Items will be the first reference workspace in a later slice; Game Version verification behavior is unchanged.

Slice 9B.3 (shared searchable record-list foundation) is complete: `RecordList` + `RecordListPagination` in `src/components/admin/` for `AdminWorkspace`'s recordList slot — URL-driven GET search (caller-owned filtering and hrefs; the component never builds routes or queries data), `aria-current` selected state in the purple editor accent, caller-supplied empty state, minimal prev/next pagination (omitted direction renders as a disabled marker, never a fake link). NO production route uses it yet.

Slice 9B.4 (Item workspace routes, record list, quick switching) is complete — Items are the first production workspace adoption: `/admin/items` is the list + guidance landing (the embedded create form is gone), `/admin/items/new` is the dedicated creation page (create-action error redirects target it), and the edit/delete routes render inside `ItemWorkspace` (`src/components/admin/item-workspace.tsx`, the thin Item-specific wrapper owning the list query and URLs over the pure `src/lib/admin/item-workspace.ts`). The record list searches name+slug (trimmed, case-insensitive, server-side, `?q=` URL-driven) and preserves `q` across row links, the create link, and back/cancel links; the open record is marked with `aria-current`. The Item URL identifier remains the SLUG. Delete is reached from the edit page's toolbar (the old table's per-row links are gone). Pagination is deliberately deferred (small record counts; `RecordListPagination` exists unused). Item forms themselves are unchanged — no tabs, panels, or sticky actions yet.

Slice 9B.5 (Item General editor) is complete: `/admin/items/new` and `/admin/items/[slug]/edit` are now composed from the shared editor primitives instead of `PageHeader` and a plain form. `EditorHeader` renders the page's one h1 — "Create item" on create, the item's own name on edit (slug as subtitle, "Manage acquisition sources" as a header action) — followed by `EditorTabs`: General is active and holds every existing core field (name, slug, description, category, held item, tradeable, base value) unchanged. On create, only General is shown (the other sections describe relations that can't exist before the record does); on edit, Acquisition Sources/Used in Recipes/Metadata render as inert `disabled` placeholders (a new optional field on the shared `EditorTab` type) — never links to empty pages. The aside column holds `ImagePanel` (the existing upload/replace/remove controls, moved unchanged, including the migrated remove-toggle CSS now in `globals.css` instead of an inline `<style>` block), `VerificationPanel` (replacing the old inline "Gameplay data verified…" sentence with the shared status badge and Verified against/Verified on rows), and on edit only, `TimestampsPanel` (created/updated/verified dates, no ids). Because `ImagePanel`/`VerificationPanel` render outside the resource's own `<form>` (in the workspace's aside slot), their inputs associate with the form via the standard HTML `form` attribute (a new optional `formId` prop threaded through `VerificationPanel` and `GameVersionVerificationControls`) — ordinary HTML submission, no client-side mutation code. Sticky `EditorActions` replaced the old form-actions row: "Create item"/Cancel on create, "Save item"/Cancel/"Delete item" (a link to the existing confirmation route) on edit, with `q` preserved throughout. Every redirect, server action, validation rule, image behavior, verification rule, and name-availability check is byte-for-byte unchanged — only presentation moved. Acquisition Sources tab content, Used in Recipes tab content, and Metadata tab content beyond `TimestampsPanel` remain unimplemented, and no other resource workspace was converted.

Slice 9B.6 (Acquisition Sources tab integration) is complete: the Acquisition Sources destination is now a real tab inside the Item workspace rather than a separate "Manage acquisition sources" header link (removed). The existing routes are unchanged — `/admin/items/[slug]/sources` (list + inline create), `/admin/items/[slug]/sources/[sourceId]/edit`, `/admin/items/[slug]/sources/[sourceId]/delete` — but all three now render inside `ItemWorkspace`, with `EditorHeader`/`EditorTabs` and the shared tab strip built by one new function, `itemEditorTabs(slug, query, active)` in `src/lib/admin/item-workspace.ts` (General and Acquisition Sources are real links with correct hrefs/active state; Used in Recipes/Metadata stay disabled placeholders). The sources list/create page wraps its existing table and inline create form in `ContextPanel`s and upgrades the create form's bare picker+checkbox to a full `VerificationPanel` (matching the Item create page's pattern); the dedicated source edit page moves `VerificationPanel` into an aside column (same `form="<id>"` association pattern as the Item General editor) and adopts `EditorActions` for Save/Cancel — button text ("Save Changes") is unchanged. The source delete page mirrors the Item delete page's precedent: no aside, no `EditorActions`, just the existing confirm-card. `ItemWorkspace` gained an optional `recordHref` prop (default `itemEditHref`) so quick-switching records while on the Acquisition Sources tab opens the next item's Acquisition Sources tab instead of its General tab — the Sources pages pass `itemSourcesHref`. Every source CRUD action, route-ownership/tampering check, and verification rule (including historical version selection and unchecked-edit preservation) is unchanged; `q` is preserved through every tab link, record-row link, and back/cancel link. Used in Recipes and Metadata tab content remain unimplemented, and no other resource workspace was converted.

Slice 9B.7 (Used in Recipes tab) is complete: `/admin/items/[slug]/recipes` is a new route rendering inside `ItemWorkspace` exactly like General/Acquisition Sources — the searchable record list stays visible with the current item selected, `EditorHeader` shows the item's own name, and `EditorTabs` (via the extended `itemEditorTabs(slug, query, active)`, whose `ItemEditorTabKey` now accepts `"recipes"`) marks Used in Recipes active. The page runs one restrained query (`prisma.item.findUnique` with both `recipesProduced` and `recipeIngredients` included alongside their `profession`/`resultingItem` relations) — no N+1 per row. Content is strictly read-only and navigational: two `ContextPanel`s ("Used as an ingredient in" and "Produced by," each with its own admin-table and a restrained count in the description, omitted entirely when empty) list recipe name (linking to the existing `/admin/recipes/[slug]/edit` route), ingredient quantity or yield, and resulting item name; there is no separate Profession/Required Level column — a shared `RecipeNameCell` renders those two optional fields as a labeled detail line ("Profession: …", "Required level: …") beneath the recipe name only when each is populated, so sparse recipes emit no placeholder dash, no empty label, and no blank cell — no inline recipe editing, no ingredient mutation, no create-recipe form. An item with no recipe relationship in either direction renders a single `EmptyState`. A new `itemUsedInRecipesHref(slug, query)` helper feeds both the tab's own href and `ItemWorkspace`'s existing `recordHref` prop, so quick-switching items while on this tab opens the next item's Used in Recipes tab (not General) with `q` preserved, matching the Acquisition Sources pattern from Slice 9B.6 exactly — no new routing framework was introduced. Recipe CRUD, ingredient actions, the Prisma schema, and authorization are all unchanged; Metadata tab content beyond `TimestampsPanel` remains unimplemented, and no other resource workspace was converted.

Slice 9B.8 (Metadata tab) is complete: `/admin/items/[slug]/metadata` is a new route rendering inside `ItemWorkspace` exactly like General/Acquisition Sources/Used in Recipes — the searchable record list stays visible with the current item selected, `EditorHeader` shows the item's own name, and `EditorTabs` (via the extended `itemEditorTabs(slug, query, active)`, whose `ItemEditorTabKey` now accepts `"metadata"`) marks Metadata active. `itemEditorTabs` no longer renders ANY disabled placeholder — every one of the four Item tabs (General, Acquisition Sources, Used in Recipes, Metadata) is now a real link, via the new `itemMetadataHref(slug, query)` helper alongside the existing three. The page runs the same `prisma.item.findUnique({ include: { verifiedGameVersion: true } })` shape the General edit page already uses, plus the same `prisma.gameVersion.findMany` ordering — no new query pattern. Content is strictly read-only: `VerificationPanel` gained a `readOnly?: boolean` prop (default `false`, fully backward-compatible with its four existing callers) that omits the composed `GameVersionVerificationControls` picker/checkbox entirely while keeping the status badge and the Verified-against/Verified-on/Current-version rows (each still hidden when its own data is absent, exactly as before); `TimestampsPanel` renders unchanged (created/updated/verified dates, no ids). The Metadata tab passes `readOnly` and renders both panels directly as the workspace's main content (no aside, no `<form>`, no `formId`) — there is no picker, checkbox, submit button, delete action, or image control anywhere on this tab, and neither panel ever surfaces the record's database id, a foreign key, or a storage path; the Item's own slug is already visible via the existing header subtitle, exactly as on every other tab. `ItemWorkspace`'s existing `recordHref` prop takes `itemMetadataHref` so quick-switching items while on this tab opens the next item's Metadata tab (not General) with `q` preserved — the same mechanism Slice 9B.6/9B.7 already established, no new routing framework. Recipe/Item CRUD, the Prisma schema, storage, and authorization are all unchanged. The Item reference workspace (General, Acquisition Sources, Used in Recipes, Metadata) is now functionally complete; no other resource workspace was converted.

Slice 9C.1 (Recipe workspace navigation foundation) is complete: Recipes are the SECOND production adoption of the shared `AdminWorkspace`/`RecordList` pieces, via a new, independent thin wrapper — `RecipeWorkspace` (`src/components/admin/recipe-workspace.tsx`) — over new pure helpers in `src/lib/admin/recipe-workspace.ts` (`recipeEditHref`, `recipeDeleteHref`, `normalizeRecipeSearchQuery`, `withRecipeSearchQuery`), mirroring `ItemWorkspace`/`item-workspace.ts` exactly but NOT sharing code with it — this is deliberately a second thin wrapper, not a generic multi-resource framework. `/admin/recipes` is now the workspace landing state (record list + restrained guidance; the embedded creation form is gone) and `/admin/recipes/new` is the dedicated creation route the form moved to unchanged (same fields, ingredient rows, image input, and `GameVersionVerificationControls` — EditorHeader/tabs/ImagePanel/VerificationPanel/TimestampsPanel/sticky EditorActions are deliberately NOT adopted this pass). `/admin/recipes/[slug]/edit` and `/admin/recipes/[slug]/delete` render inside `RecipeWorkspace` unchanged otherwise (same PageHeader, same form, same confirm-card); the edit page's toolbar gained a "Delete Recipe" link (replacing the old table's per-row Delete action) placed OUTSIDE the ingredient-count guard so deletion stays reachable even when that guard hides the edit form entirely. The record list shows the recipe name as primary text and the resulting item's name as secondary context; search matches name OR slug, trimmed, case-insensitive, server-rendered via `?q=`, preserved through record links, the create link, and Cancel/Delete links. Because the create form moved, `createRecipeAction`'s pre-creation validation/duplicate/relation/verification/image error redirects now target `/admin/recipes/new` instead of `/admin/recipes` (mirroring exactly how `createItemAction` was changed for Slice 9B.4) — the success redirect and every other action (`updateRecipeAction`, `deleteRecipeAction`), the Prisma schema, ingredient handling, image storage, and Game Version verification are all byte-for-byte unchanged. Pagination is deliberately deferred (small record count, matching the Item precedent). No Recipe tabs, no Ingredients split, no Professions/Categories/Locations conversion, no dashboard summaries, and no Route Hubs were started.

Slice 9C.2 (Recipe General editor conversion) is complete: `/admin/recipes/new` and `/admin/recipes/[slug]/edit` are now composed from the shared editor primitives instead of `PageHeader` and a plain form, mirroring the Item General editor's Slice 9B.5 conversion exactly. `EditorHeader` renders the page's one h1 — "Create Recipe" on create, the recipe's own name on edit (slug as subtitle) — followed by `EditorTabs`: on create, only General is shown (matching the Item precedent — Ingredients and Metadata describe relations/history that can't exist before the record does, so they are omitted rather than shown disabled); on edit, Ingredients and Metadata render as inert `disabled` placeholders via the new `recipeEditorTabs(slug, query)` helper in `src/lib/admin/recipe-workspace.ts` (structurally identical to `itemEditorTabs` but Recipe-specific — no shared cross-resource tab framework was introduced). Ingredients remain embedded in General's own fields; splitting them into a real tab is a later slice. The aside column holds `ImagePanel` (the existing upload/replace/remove controls, moved unchanged; the Recipe-specific inline `<style>` block for the remove-toggle was deleted in favor of the exact same shared `.admin-image-remove-*` classes Item already migrated to in Slice 9B.5 — verified byte-for-byte equivalent before reuse), `VerificationPanel` (replacing the bare `GameVersionVerificationControls` call with the shared status badge and Verified against/Verified on rows), and on edit only, `TimestampsPanel` (created/updated/verified dates, no ids). Because `ImagePanel`/`VerificationPanel` render outside the resource's own `<form>` (in the workspace's aside slot), their inputs associate via the standard HTML `form` attribute (`RECIPE_CREATE_FORM_ID`/`RECIPE_EDIT_FORM_ID`) — ordinary HTML submission, no client-side mutation code. Sticky `EditorActions` replaced the old `form-actions` row: "Create Recipe"/Cancel on create, "Save Changes"/Cancel on edit — Delete is deliberately NOT passed to `EditorActions`' own `deleteHref`; it instead lives in `EditorHeader`'s `actions` slot (unconditionally rendered, outside the `children` region the too-many-ingredients guard replaces), preserving the exact reachability guarantee Slice 9C.1's toolbar link established. That same guard now also withholds `ImagePanel`/`VerificationPanel` specifically (their controls have no form to submit into once the guard hides it — matching the pre-conversion behavior where the whole form, image and verification included, was replaced by the alert), while `TimestampsPanel` — pure read-only display of already-loaded data — still renders regardless. Every redirect, server action, validation rule, ingredient parsing/dedup/capacity-guard, image behavior, and verification rule is byte-for-byte unchanged — only presentation moved. No Ingredients tab, no Metadata content, and no other resource workspace was converted.

Slice 9C.3 (Recipe Ingredients tab) is complete: `/admin/recipes/[slug]/ingredients` is a new route rendering inside `RecipeWorkspace` exactly like General — the record list stays visible with the current recipe selected, `EditorHeader` shows the recipe's own name, and `EditorTabs` (via `recipeEditorTabs(slug, query, active)`, now taking an `active: "general" | "ingredients"` key exactly like `itemEditorTabs`) marks Ingredients active while General becomes a real link back. General's ingredient `<fieldset>` is gone entirely — moved unchanged (same markup, same five fixed rows, same item-name prefill order) to the new route — and General's `tooManyIngredients` guard is gone too: General is now ALWAYS fully editable, a deliberate improvement over the prior all-or-nothing behavior, since the guard's only reason to exist was protecting ingredient rows that no longer live there. The guard moved to Ingredients instead, using the identical alert wording (adjusted from "the edit form" to "this form"), and Ingredients' `EditorHeader` carries its own unconditional Delete Recipe `actions` link — the same reachability guarantee Slice 9C.1/9C.2 established, now doubled (both General and Ingredients can always reach Delete). Ingredients renders no `ImagePanel`/`VerificationPanel`/`TimestampsPanel` — none apply to ingredient rows. Server-side, `updateRecipeAction` was replaced by two narrow actions in `src/app/admin/recipes/actions.ts`: `updateRecipeGeneralAction` (a single `prisma.recipe.update()` — name/slug/resultingItemId/resultingQuantity/professionId/requiredLevel/image/verification stamp — never touching `RecipeIngredient`) and `updateRecipeIngredientsAction` (a `prisma.$transaction([deleteMany, createMany])` — the exact delete-then-recreate shape the old action used, but scoped to `RecipeIngredient` only, plus a defense-in-depth re-check that a tampered request can never truncate an already-over-capacity recipe down to the form's 5-row limit). Both reuse — never reimplement — validation: `src/lib/validation/recipe.ts` now extracts a private `parseRecipeGeneralFields` helper that `parseRecipeInput` (unchanged, still the create page's parser), the new exported `parseRecipeGeneralInput`, and the new exported `parseRecipeIngredientsInput` (wrapping the existing private `parseIngredientRows`) all share — the same field-by-field rules, same error codes, same row limit, same duplicate-item detection, in exactly one place. `RecipeWorkspace` gained an optional `recordHref` prop (default `recipeEditHref`, mirroring `ItemWorkspace`) so quick-switching recipes while on the Ingredients tab opens the next recipe's Ingredients tab instead of dropping back to General — the Ingredients page passes `recipeIngredientsHref`. Every validation rule, duplicate/quantity/capacity check, transaction shape, redirect, and revalidation footprint is preserved; the create page and its embedded ingredients are completely untouched. Metadata content remains unimplemented; no other resource workspace was converted.

Every other resource workspace conversion and dashboard summaries have not started. Route Hubs remain deferred and unstarted. Deployment — previously listed as Milestone 8 — is a later milestone, is not complete, and has not started. Do not begin any of these until explicitly instructed in the milestone conversation.

Milestone 8 - Gameplay Data Expansion is complete (see DECISIONS.md 2026-07-16): rarity removed and the exact `Held item` Yes/No field added; full ten-profession coverage with the in-place Blacksmithing → Smithing rename; the Location hierarchy with restricted deletion; the AcquisitionSource model with the public "How to obtain" section; item/source route-ownership enforcement; and the rule that public detail pages never render empty optional sections.

The automated test stack is backed by an isolated Supabase test project and a fail-closed `.env.test.local` guard. Migrations reach the test project only through the guarded `pnpm test:db:migrate`. The isolated test database carries one documented test-only Game Version fixture, `test-gv-current`, (re)made current by the E2E auth setup.

The Supabase security warning is resolved: the Data API is disabled on both projects, and Prisma over direct PostgreSQL remains the only game-data access layer (see DECISIONS.md 2026-07-15).

## Test Commands

- `pnpm test:unit`
- `pnpm test:env:check`
- `pnpm test:db:migrate` (guarded `prisma migrate deploy` against the isolated test project)
- `pnpm test:integration`
- `pnpm test:service`
- `pnpm test:e2e`

Follow the test execution cadence recorded in DECISIONS.md (2026-07-15): targeted runs during implementation, the relevant suites plus lint/build/diff-check before a checkpoint commit, and the full stack plus the preservation audit before a milestone completion or major push.

## Working Style

- Assume the project owner is a beginner.
- Explain commands before suggesting them.
- Make small, reviewable changes.
- Do not skip setup verification.
- Do not invent scope.
- Do not add packages without explicit approval.
- Do not create database/auth/deployment/admin features before the relevant milestone.
- After each meaningful change, recommend checking:
  - git status
  - relevant version/test command

## PowerShell command style

Run routine Git and verification commands as separate PowerShell tool calls.

Avoid combining unrelated commands with semicolons or pipelines solely to reduce tool calls.

Do not use expandable strings containing variables such as
`"exit:$LASTEXITCODE"` when the command's native exit status is sufficient.

Prefer:

- `git diff --check`
- `git status --short -uall`
- `git log -8 --oneline`
- `git rev-parse HEAD`
- `git rev-parse origin/main`
- `git check-ignore .env.test.local`
- `git ls-files --error-unmatch next-env.d.ts`

as individual commands.

## Expected Stack

- Next.js
- TypeScript
- Tailwind CSS
- shadcn/ui
- Supabase
- Prisma
- Vercel
- GitHub
- pnpm
