# PokeForce Companion - AI Rules

## Source of Truth

The source of truth for this project is:

1. PokeForce Companion Project Brief
2. PokeForce Companion Technical Brief
3. Current milestone instructions from ChatGPT

AI tools must not invent features, architecture, database tables, or design systems outside the confirmed scope.

## Current Scope

The current phase is:

Milestone 9 - Admin Workspace & Game Version Management is complete (finalized 2026-07-20; see MILESTONES.md and DECISIONS.md). Slice 9A (Game Version foundation) is complete (see MILESTONES.md and DECISIONS.md 2026-07-17): the database-backed GameVersion model with at most one current version (advisory-lock-serialized service transactions); relational verification (`verifiedGameVersionId`, ON DELETE RESTRICT) on Item, Location, AcquisitionSource, Recipe, and Profession (Categories deliberately have none); verification against a selected Game Version — the shared picker on every verification form submits a server-validated id, historical versions are valid selections, and a blank selection falls back to the current version; the retirement of the CURRENT_GAME_BUILD_ID environment variable; and admin-only Game Version management as a secondary settings destination (`/admin/settings/game-versions`) — never in primary navigation, never public.

Terminology: the contributor-facing term is "Game Version" everywhere. Never display "Build" in the UI.

Slice 9B.1 (shared admin shell and persistent navigation) is complete: every admin route renders inside the shared `AdminShell` with exactly six primary sidebar destinations (Dashboard, Items, Recipes, Professions, Categories, Locations). Game Versions remain a secondary settings destination; Acquisition Sources remain contextual under their owning item. Resource workspace conversion is still pending.

Slice 9B.2 (shared admin editor primitives) is complete: resource-agnostic editor header, tabs, context/image/verification/timestamps panels, and sticky actions exist in `src/components/admin/` — no resource page adopts them yet. Items will be the first reference workspace. Game Version behavior is unchanged.

Slice 9B.3 (shared searchable record-list foundation) is complete: the resource-agnostic `RecordList` and `RecordListPagination` components exist with URL-driven search and caller-owned filtering — no production route adopts them yet. Items remains the first reference workspace.

Slice 9B.4 (Item workspace adoption) is complete: `/admin/items` is the searchable record-list landing, `/admin/items/new` the dedicated creation route, and Item edit/delete render inside the workspace with URL-driven (`?q=`) server-rendered search and quick switching. Item forms, CRUD actions, images, and verification behavior are unchanged; the full Item editor redesign (tabs/panels/sticky actions) is still pending.

Slice 9B.5 (Item General editor) is complete: `/admin/items/new` and `/admin/items/[slug]/edit` now compose the shared editor primitives — `EditorHeader` (one h1: "Create item" or the item's own name), `EditorTabs` (General active; on edit, Acquisition Sources/Used in Recipes/Metadata render as inert disabled placeholders, never links to empty pages; on create, only General is shown), `ImagePanel`, `VerificationPanel`, `TimestampsPanel` (edit only), and sticky `EditorActions` ("Create item"/"Save item", Cancel, and on edit a "Delete item" link to the existing confirmation route). Every Item field, redirect, server action, image behavior, verification rule, and name-availability check is unchanged — only the presentation moved. Acquisition Sources tab content, Used in Recipes tab content, and Metadata tab content beyond `TimestampsPanel` remain unimplemented. No other resource workspace was converted.

Slice 9B.6 (Acquisition Sources tab integration) is complete: Acquisition Sources is now a real, working tab in the Item workspace (`itemEditorTabs` in `src/lib/admin/item-workspace.ts` builds the shared tab strip for every Item route). The former "Manage acquisition sources" header action is gone. The existing routes (`/admin/items/[slug]/sources` and its `[sourceId]/edit`/`delete` children) are unchanged and now render inside `ItemWorkspace` with the tab strip, the record list, and (on the source edit page) a `VerificationPanel` aside. Quick switching between items while on the Acquisition Sources tab opens the next item's Acquisition Sources tab, not General, via a new `recordHref` prop on `ItemWorkspace`. Every source CRUD action, ownership/tampering protection, and verification rule is unchanged. Used in Recipes and Metadata tab content remain unimplemented.

Slice 9B.7 (Used in Recipes tab) is complete: `/admin/items/[slug]/recipes` is a real, read-only Item tab showing every Recipe relationship for the selected Item — "Used as an ingredient in" and "Produced by," never conflated, each row linking to the existing Recipe admin edit route with no inline editing. `itemEditorTabs` now marks Used in Recipes active on this route; only Metadata remains a disabled placeholder. Quick switching between items while on this tab opens the next item's Used in Recipes tab via the same `recordHref` mechanism from Slice 9B.6.

Slice 9B.8 (Metadata tab) is complete: `/admin/items/[slug]/metadata` is the fourth and final real Item tab, showing restrained, read-only administrative information — `VerificationPanel` (now supporting a `readOnly` option that omits the composed picker/checkbox entirely) for status/verified-version/verified-date/current-version, and `TimestampsPanel` for created/updated/verified dates. No form, picker, checkbox, submit button, delete action, or image control exists on this tab; no database id, foreign key, or storage path is ever shown. `itemEditorTabs` now links every Item tab as a real destination — no Item tab remains disabled. The Item reference workspace (General, Acquisition Sources, Used in Recipes, Metadata) is functionally complete.

Slice 9C.1 (Recipe workspace navigation foundation) is complete: Recipes are the SECOND production adoption of `AdminWorkspace`/`RecordList`, following the Item workspace's precedent from Slice 9B.4. `/admin/recipes` is the searchable record-list landing (no embedded creation form); `/admin/recipes/new` is the dedicated creation route; `/admin/recipes/[slug]/edit` and `/admin/recipes/[slug]/delete` render inside the new `RecipeWorkspace`. Recipe search is URL-driven (`?q=`), trimmed, case-insensitive on name/slug, server-rendered — no live per-keystroke requests.

Slice 9C.2 (Recipe General editor conversion) is complete: `/admin/recipes/new` and `/admin/recipes/[slug]/edit` now compose the shared editor primitives — `EditorHeader` (one h1: "Create Recipe" or the recipe's own name), `EditorTabs` (General active; create shows only General with no placeholders, matching the Item precedent; edit adds Ingredients/Metadata as disabled placeholders via the new `recipeEditorTabs` helper), `ImagePanel`, `VerificationPanel`, `TimestampsPanel` (edit only), and sticky `EditorActions` ("Create Recipe"/"Save Changes", Cancel). Delete moved into `EditorHeader`'s `actions` slot rather than `EditorActions`' own `deleteHref`, so it stays reachable even when the too-many-ingredients guard hides the entire form — `ImagePanel`/`VerificationPanel` are withheld in that same guarded state (their controls have nothing to submit into), while `TimestampsPanel` (pure read-only) still renders. Every Recipe field, ingredient row, image behavior, verification rule, and name-availability check is unchanged — only the presentation moved. Ingredients remain embedded in General fields (no dedicated tab yet); Metadata content remains unimplemented. Professions, Categories, and Locations remain unconverted.

Slice 9C.3 (Recipe Ingredients tab) is complete: `/admin/recipes/[slug]/ingredients` is a new, real, independent Recipe tab holding the ingredient rows moved out of General. `/admin/recipes/[slug]/edit` (General) no longer renders ingredient rows and no longer carries the too-many-ingredients guard at all — it is always fully editable regardless of ingredient count, a deliberate improvement over the prior all-or-nothing behavior. The guard now applies to the Ingredients tab only, and Delete Recipe stays reachable there too. `updateRecipeAction` was replaced by two narrow actions — `updateRecipeGeneralAction` (every Recipe field except ingredients) and `updateRecipeIngredientsAction` (ingredient rows only) — sharing their field/row validation via new `parseRecipeGeneralInput`/`parseRecipeIngredientsInput` functions extracted from the unchanged `parseRecipeInput` (still used, unmodified, by the create page). Neither action ever touches the other's data: a General save leaves ingredients byte-for-byte untouched, and an ingredients save leaves name/slug/resulting item/profession/required level/image/verification byte-for-byte untouched. The create page is unaffected — ingredients remain embedded there until the record exists. Metadata content remains unimplemented; Professions, Categories, and Locations remain unconverted.

Slice 9C.4 (Recipe Metadata tab) is complete: `/admin/recipes/[slug]/metadata` is the third and final real Recipe tab, mirroring the Item workspace's Slice 9B.8 completion. `recipeEditorTabs` now links every Recipe tab (General, Ingredients, Metadata) as a real destination via the new `recipeMetadataHref` helper — no Recipe tab remains disabled. The tab is strictly read-only: a `ContextPanel` titled "Recipe" shows the resulting item, the optional Profession and required level (each omitted entirely when absent — no placeholder dash, no "None"), and the ingredient count; `VerificationPanel` renders in its existing `readOnly` mode (status badge and stamp rows, no picker or checkbox); `TimestampsPanel` shows created/updated/verified dates. No form, image control, ingredient control, or delete action exists on this tab, and no database id, foreign key, or storage path is ever shown. The one query loads `resultingItem`, `profession`, `verifiedGameVersion`, and an ingredient `_count` — never the full ingredient rows. The Recipe reference workspace (General, Ingredients, Metadata) is now functionally complete, matching the Item workspace's shape. Professions, Categories, and Locations remain unconverted.

Slice 9D.1 (Profession workspace navigation foundation) is complete: Professions are the THIRD production adoption of `AdminWorkspace`/`RecordList`, following the Item (Slice 9B.4) and Recipe (Slice 9C.1) workspaces' precedent exactly — a new, independent `ProfessionWorkspace` over pure helpers in `src/lib/admin/profession-workspace.ts`, not a shared multi-resource framework. `/admin/professions` is the searchable record-list landing (no embedded creation form; the record list's secondary context shows each profession's recipe count via a `_count` select, never the full relation, so no N+1 query is introduced); `/admin/professions/new` is the dedicated creation route the form moved to unchanged (same fields, image input, and `GameVersionVerificationControls` — EditorHeader/tabs/panels/sticky EditorActions are deliberately NOT adopted this pass, matching Slice 9C.1's own restraint); `/admin/professions/[slug]/edit` and `/admin/professions/[slug]/delete` render inside `ProfessionWorkspace` unchanged otherwise (same PageHeader, same form, same confirm-card). The edit page's toolbar gained a "Delete Profession" link (replacing the old table's per-row Delete action, mirroring the Recipe precedent). Profession search is URL-driven (`?q=`), trimmed, case-insensitive on name/slug, server-rendered — no live per-keystroke requests; pagination is deliberately deferred (ten seeded professions, smaller than the Item/Recipe precedent that already deferred it). Because the create form moved, `createProfessionAction`'s error redirects now target `/admin/professions/new` instead of `/admin/professions`; `updateProfessionAction`/`deleteProfessionAction` and every other action, the Prisma schema, image storage, verification behavior, and the recipe-linked delete-blocking rule are all byte-for-byte unchanged. No Profession tabs, no Recipes relationship tab, no shared editor panels, and no other resource workspace (Categories, Locations) was converted.

Slice 9D.2 (Profession General editor conversion) is complete: `/admin/professions/new` and `/admin/professions/[slug]/edit` now compose the shared editor primitives — `EditorHeader` (one h1: "Create Profession" or the profession's own name), `EditorTabs` (General active; create shows only General with no placeholders, matching the Item/Recipe precedent; edit adds Recipes/Metadata as disabled placeholders via the new `professionEditorTabs` helper), `ImagePanel` (reusing the exact shared `.admin-image-remove-*` classes Item/Recipe already migrated to — no bespoke inline `<style>` block), `VerificationPanel`, `TimestampsPanel` (edit only), and sticky `EditorActions` ("Create Profession"/"Save Changes", Cancel, and on edit a "Delete Profession" link via `EditorActions`' own `deleteHref` — Professions carry no capacity guard that would ever need to withhold it, unlike Recipe's Ingredients-driven guard). Every Profession field (name, slug, description), image behavior, verification rule, and name-availability check is unchanged — only the presentation moved. Recipes (a relationship tab) and Metadata content remained unimplemented at this point. Categories and Locations remain unconverted.

Slice 9D.3 (Profession Recipes relationship tab) is complete: `/admin/professions/[slug]/recipes` is a new, real, read-only tab inside `ProfessionWorkspace`, mirroring the Item workspace's Used in Recipes tab (Slice 9B.7) shape but for a single relationship direction (the Recipes linked to this Profession). `professionEditorTabs` now takes an `active: "general" | "recipes"` key (structurally identical to `itemEditorTabs`/`recipeEditorTabs`) so both General and Recipes are real links; Metadata remains the only disabled placeholder at this point. A new `professionRecipesHref(slug, query)` helper feeds both the tab's own href and `ProfessionWorkspace`'s new optional `recordHref` prop (mirroring `ItemWorkspace`'s), so quick-switching professions while on this tab opens the next profession's Recipes tab (not General) with `q` preserved. The page runs one restrained query, `prisma.profession.findUnique({ include: { recipes: { include: { resultingItem: true }, orderBy: { name: "asc" } } } })` — no per-row follow-up query — and renders a `ContextPanel` table (Recipe/Resulting Item/Quantity) when recipes exist, or a restrained `EmptyState` when none do. Each recipe name links to the EXISTING `/admin/recipes/[slug]/edit` route (no Profession `q` appended to that link); Required level renders as a labeled detail line beneath the recipe name only when present, following the same hide-empty convention Item's Used in Recipes tab established (no placeholder dash, no "None", no blank cell) — unlike that tab's cell, no Profession name is repeated here, since the current Profession is already the page context. The tab contains no form, input, select, checkbox, file control, or mutation/delete/image/verification control anywhere. Profession/Recipe actions, the Prisma schema, storage, and authorization are all unchanged; Metadata content remained unimplemented at this point, and no other resource workspace was converted.

Slice 9D.4 (Profession Metadata tab) is complete: `/admin/professions/[slug]/metadata` is a new, real, read-only tab inside `ProfessionWorkspace`, completing the Profession workspace — mirroring the Item (Slice 9B.8) and Recipe (Slice 9C.4) Metadata tabs' shape exactly. `professionEditorTabs`'s `ProfessionEditorTabKey` now accepts `"metadata"` and `professionEditorTabs` no longer renders any disabled placeholder — every one of the three Profession tabs (General, Recipes, Metadata) is a real link, via the new `professionMetadataHref(slug, query)` helper alongside the existing two. `ProfessionWorkspace`'s existing `recordHref` prop takes `professionMetadataHref` so quick-switching professions while on this tab opens the next profession's Metadata tab (not General), with `q` preserved. The page runs one restrained query, `prisma.profession.findUnique({ include: { verifiedGameVersion: true, _count: { select: { recipes: true } } } })` — the same `verifiedGameVersion` relation the General edit page already loads, plus a Recipe `_count` (never the full `recipes` relation, which the Recipes tab already covers) — no per-row follow-up query. Content is strictly read-only: a `ContextPanel` titled "Profession" shows the Recipe count (via `_count`, always rendered since zero is itself meaningful administrative context); `VerificationPanel` reuses its existing `readOnly` prop (introduced in Slice 9B.8, unchanged) to omit the composed picker/checkbox while keeping the status badge and Verified-against/Verified-on/Current-version rows (each still hidden when its own data is absent); `TimestampsPanel` renders unchanged (created/updated/verified dates, no ids). The description field is deliberately NOT repeated here — it is an editable General field, and duplicating it would add no administrative information this tab exists to show. There is no picker, checkbox, submit button, delete action, image control, or Recipe-relationship control anywhere on this tab, and none of the three panels ever surfaces the record's database id, a foreign key, or a storage path; the Profession's own slug is already visible via the existing header subtitle, exactly as on every other tab. General/Recipes behavior, Profession/Recipe actions, the Prisma schema, storage, and authorization are all unchanged. The Profession workspace (General, Recipes, Metadata) is now functionally complete, matching the Item and Recipe workspaces' shape; no other resource workspace was converted.

Category workspace navigation foundation is complete: Categories are the FOURTH production adoption of `AdminWorkspace`/`RecordList`, following the Item, Recipe, and Profession workspaces' precedent exactly — a new, independent `CategoryWorkspace` over pure helpers in `src/lib/admin/category-workspace.ts`, not a shared multi-resource framework. `/admin/categories` is the searchable record-list landing (no embedded creation form; the record list's secondary context shows each category's linked item count via a `_count` select, never the full relation, so no N+1 query is introduced); `/admin/categories/new` is the dedicated creation route the form moved to unchanged (same fields — name, slug, description); `/admin/categories/[slug]/edit` and `/admin/categories/[slug]/delete` render inside `CategoryWorkspace` unchanged otherwise (same PageHeader, same form, same confirm-card) at this point. The edit page's toolbar gained a "Delete Category" link (replacing the old table's per-row Delete action, mirroring the Profession precedent). Category search is URL-driven (`?q=`), trimmed, case-insensitive on name/slug, server-rendered — no live per-keystroke requests; pagination is deliberately deferred (five seeded categories, the smallest record count of any converted resource so far). Because the create form moved, `createCategoryAction`'s error redirects now target `/admin/categories/new` instead of `/admin/categories`; `updateCategoryAction`/`deleteCategoryAction` and every other behavior, the Prisma schema, and the item-linked delete-blocking rule are all byte-for-byte unchanged. No Category tabs, no Items relationship tab, no shared editor primitives were adopted at this point, and Locations remain unconverted.

Category General editor conversion is complete: `/admin/categories/new` and `/admin/categories/[slug]/edit` now compose the shared editor primitives — `EditorHeader` (one h1: "Create Category" or the category's own name, slug as subtitle on edit), `EditorTabs` (General active; create shows only General with no placeholders, matching the Item/Recipe/Profession precedent; edit adds Items/Metadata as disabled placeholders via the new `categoryEditorTabs` helper), `TimestampsPanel` (edit only: Created/Updated — Categories carry no verification stamp, so no Verified row exists), and sticky `EditorActions` ("Create Category"/"Save Changes", Cancel, and on edit a "Delete Category" link via `EditorActions`' own `deleteHref` — Categories carry no capacity guard that would ever need to withhold it). Categories have no image or gameplay-verification behavior, so — unlike Item/Recipe/Profession — this conversion deliberately adds NO `ImagePanel`, NO `VerificationPanel`, and NO `GameVersionVerificationControls`; the aside on create is omitted entirely (no panel applies pre-save), and the edit aside holds only `TimestampsPanel`. Every Category field (name, slug, description), redirect, server action, duplicate-name check, and the item-linked delete-blocking rule is unchanged — only the presentation moved. Items (a relationship tab) and Metadata content remained unimplemented at this point. Locations remain unconverted.

Slice 9E.3 (Category Items relationship tab) is complete: `/admin/categories/[slug]/items` is a new, real, read-only tab inside `CategoryWorkspace`, mirroring the Profession workspace's own Recipes tab (Slice 9D.3) shape exactly for a single relationship direction (the Items linked to this Category). `categoryEditorTabs` now takes an `active: "general" | "items"` key (structurally identical to `professionEditorTabs`) so both General and Items are real links; Metadata remains the only disabled placeholder. A new `categoryItemsHref(slug, query)` helper feeds both the tab's own href and `CategoryWorkspace`'s new optional `recordHref` prop (mirroring `ProfessionWorkspace`'s), so quick-switching categories while on this tab opens the next category's Items tab (not General) with `q` preserved. The page runs one restrained query, `prisma.category.findUnique({ include: { items: { orderBy: { name: "asc" } } } })` — no per-row follow-up query — and renders a `ContextPanel` table (Item/Held Item/Tradeable) when items exist, or a restrained `EmptyState` when none do. Each item name links to the EXISTING `/admin/items/[slug]/edit` route (no Category `q` appended to that link); Base value renders as a labeled detail line beneath the item name only when present, following the same hide-empty convention Profession's Recipes tab established (no placeholder dash, no "None", no blank cell) — Held item and Tradeable are always-meaningful booleans, so they render as explicit Yes/No columns rather than optional detail lines. The tab contains no form, input, select, checkbox, file control, or mutation/delete/image/verification control anywhere. Category/Item actions, the Prisma schema, storage, and authorization are all unchanged; Metadata content remained unimplemented at this point, and Locations remain unconverted.

Slice 9E.4 (Category Metadata tab) is complete: `/admin/categories/[slug]/metadata` is a new, real, read-only tab inside `CategoryWorkspace`, completing the Category workspace — mirroring the Item (Slice 9B.8), Recipe (Slice 9C.4), and Profession (Slice 9D.4) Metadata tabs' shape but deliberately LEANER, since Categories carry no image or gameplay-verification behavior. `categoryEditorTabs`'s `CategoryEditorTabKey` now accepts `"metadata"` and `categoryEditorTabs` no longer renders any disabled placeholder — every one of the three Category tabs (General, Items, Metadata) is a real link, via the new `categoryMetadataHref(slug, query)` helper alongside the existing two. `CategoryWorkspace`'s existing `recordHref` prop takes `categoryMetadataHref` so quick-switching categories while on this tab opens the next category's Metadata tab (not General), with `q` preserved. The page runs one restrained query, `prisma.category.findUnique({ include: { _count: { select: { items: true } } } })` — an Item `_count` only, never the full `items` relation, which the Items tab already covers — no per-row follow-up query, and NO `gameVersions` query, unlike Item/Recipe/Profession's own Metadata tabs. Content is strictly read-only: a `ContextPanel` titled "Category" shows the Item count (via `_count`, always rendered since zero is itself meaningful administrative context); `TimestampsPanel` shows created/updated dates only (no ids, no Verified row, since Categories carry no verification stamp) — no `VerificationPanel`, no `ImagePanel`, and no `GameVersionVerificationControls` exist on this tab. The description field is deliberately NOT repeated here — it is an editable General field, and duplicating it would add no administrative information this tab exists to show. There is no picker, checkbox, submit button, delete action, image control, verification control, or Item-relationship control anywhere on this tab, and neither panel ever surfaces the record's database id, a foreign key, or a storage path; the Category's own slug is already visible via the existing header subtitle, exactly as on every other tab. General/Items behavior, Category/Item actions, the Prisma schema, storage, and authorization are all unchanged. The Category workspace (General, Items, Metadata) is now functionally complete, matching the Item, Recipe, and Profession workspaces' shape; Locations remain unconverted.

Slice 9F.1 (Location workspace navigation foundation) is complete: Locations are the FIFTH production adoption of `AdminWorkspace`/`RecordList`, following the Item, Recipe, Profession, and Category workspaces' precedent exactly — a new, independent `LocationWorkspace` over pure helpers in `src/lib/admin/location-workspace.ts`, not a shared multi-resource framework. `/admin/locations` is the searchable record-list landing (no embedded creation form; the record list's secondary context shows each location's type label, plus its parent's name when one exists, loaded via `include: { parent: true }` in the same query — no N+1); `/admin/locations/new` is the dedicated creation route the form moved to unchanged (same fields — name, slug, type, parent, description, access note, image, `GameVersionVerificationControls` — EditorHeader/tabs/panels/sticky EditorActions are deliberately NOT adopted this pass); `/admin/locations/[slug]/edit` and `/admin/locations/[slug]/delete` render inside `LocationWorkspace` unchanged otherwise (same PageHeader, same form, same confirm-card). The edit page's toolbar gained a "Delete Location" link (replacing the old table's per-row Delete action, mirroring the Category/Profession precedent). Location search is URL-driven (`?q=`), trimmed, case-insensitive on name/slug, plus a clean, predictable type-label match against the fixed `LOCATION_TYPE_LABELS` lookup — server-rendered, no live per-keystroke requests; pagination is deliberately deferred (zero seeded locations, even smaller than Category's own 5). No tree control, expandable hierarchy, or nested navigation was built — the list stays flat and alphabetical, matching every other converted resource. Because the create form moved, `createLocationAction`'s error redirects now target `/admin/locations/new` instead of `/admin/locations`; `updateLocationAction`/`deleteLocationAction` and every other behavior — hierarchy/cycle-prevention rules, image storage, Game Version verification, and the child-location delete-blocking rule — are all byte-for-byte unchanged. No Location tabs (General/Hierarchy/Acquisition Sources/Metadata split), no shared editor primitives were adopted at this point.

Slice 9F.2 (Location General editor conversion) is complete: `/admin/locations/new` and `/admin/locations/[slug]/edit` now compose the shared editor primitives — `EditorHeader` (one h1: "Create Location" or the location's own name), `EditorTabs` (General active; create shows only General with no placeholders, matching the Item/Recipe/Profession/Category precedent; edit adds Hierarchy/Acquisition Sources/Metadata as disabled placeholders via the new `locationEditorTabs` helper), `ImagePanel` (reusing the exact shared `.admin-image-remove-*` classes Item/Recipe/Profession already migrated to — no bespoke inline `<style>` block), `VerificationPanel`, `TimestampsPanel` (edit only), and sticky `EditorActions` ("Create Location"/"Save Changes", Cancel, and on edit a "Delete Location" link via `EditorActions`' own `deleteHref` — Locations carry no capacity guard that would ever need to withhold it). Parent selection stays exactly where it already was — inside General's own fields — since splitting hierarchy editing into a dedicated tab is later work; General still does not load the full `children` or `acquisitionSources` relations. Every Location field (name, slug, type, parent, description, access note), image behavior, verification rule (including historical Game Version selection and unchecked-edit preservation), hierarchy/cycle-prevention rule, and the child-location delete-blocking rule is unchanged — only the presentation moved. Hierarchy, Acquisition Sources, and Metadata tab content remained unimplemented at this point.

Slice 9F.3 (Location Hierarchy tab) is complete: `/admin/locations/[slug]/hierarchy` is a new, real tab inside `LocationWorkspace` — `locationEditorTabs` now takes an `active: "general" | "hierarchy"` key (mirroring `professionEditorTabs`'s Slice 9D.3 shape) so both General and Hierarchy are real links; Acquisition Sources and Metadata remain the only disabled placeholders. Parent assignment moved OUT of General's own fields and into this tab — the "Parent location" `<select>` no longer appears on `/admin/locations/[slug]/edit` at all (create is unaffected, since it never had a General/Hierarchy split to begin with). `updateLocationAction` was replaced by two narrow actions: `updateLocationGeneralAction` (every General field, never `parentId`) and `updateLocationHierarchyAction` (a single update touching ONLY `parentId`) — both reuse the exact same field validation and the existing `wouldCreateLocationCycle` guard, never a second implementation. The Hierarchy tab holds a narrow parent-selection form (self excluded, sticky `EditorActions` with "Save Hierarchy"/Cancel/"Delete Location") plus a read-only `ContextPanel` listing this location's DIRECT children only (one restrained query, alphabetically ordered, never a recursive descendant walk), each linking to the EXISTING General edit route with its type label as context; an `EmptyState` replaces the panel entirely when there are no children. `LocationWorkspace` gained a `recordHref` prop (mirroring `ProfessionWorkspace`'s own) so quick-switching locations while on Hierarchy stays on Hierarchy, with `q` preserved. Every hierarchy/cycle-prevention rule, image behavior, verification behavior, and the child-location delete-blocking rule is byte-for-byte unchanged — dedicated integration tests now prove a General save can never alter `parentId` and a Hierarchy save can never alter any other field. Acquisition Sources and Metadata tab content remain unimplemented.

Slice 9F.4 (Location Acquisition Sources tab) is complete: `/admin/locations/[slug]/sources` is a new, real, read-only tab inside `LocationWorkspace` — `locationEditorTabs`'s `LocationEditorTabKey` now accepts `"sources"` via the new `locationSourcesHref` helper; only Metadata remains a disabled placeholder. Mirroring the Profession Recipes/Category Items tabs exactly, this tab is a pure relationship VIEW: Acquisition Source mutations stay entirely Item-owned, reachable only through the existing `/admin/items/[slug]/sources` routes — no create/edit/unlink/delete control exists here. One restrained query loads the location's linked Acquisition Sources (Item and Profession included, no per-row follow-up); rows are grouped by `AcquisitionType` in the enum's own declared order (via the new `sortLocationAcquisitionSourcesByType` helper, reusing `ACQUISITION_TYPES` — never a second ordering) then by item name, with source label/profession/quantity/notes each shown only when present (no placeholder dash, no blank cell). Each row links to the EXISTING Item-owned source edit route, with no Location `q` carried onto that cross-resource URL. `LocationWorkspace`'s `recordHref` prop takes `locationSourcesHref` so quick-switching stays on this tab, with `q` preserved. Location/Item/AcquisitionSource CRUD, the Prisma schema, and authorization are all unchanged. Metadata tab content remains unimplemented.

Slice 9F.5 (Location Metadata tab) is complete: `/admin/locations/[slug]/metadata` is a new, real, read-only tab inside `LocationWorkspace`, completing the Location workspace — mirroring the Item (Slice 9B.8), Recipe (Slice 9C.4), Profession (Slice 9D.4), and Category (Slice 9E.4) Metadata tabs' shape exactly. `locationEditorTabs`'s `LocationEditorTabKey` now accepts `"metadata"` and `locationEditorTabs` no longer renders any disabled placeholder — every one of the four Location tabs (General, Hierarchy, Acquisition Sources, Metadata) is a real link, via the new `locationMetadataHref(slug, query)` helper alongside the existing three. `LocationWorkspace`'s existing `recordHref` prop takes `locationMetadataHref` so quick-switching locations while on this tab opens the next location's Metadata tab (not General), with `q` preserved. The page runs one restrained query, `prisma.location.findUnique({ include: { parent: true, verifiedGameVersion: true, _count: { select: { children: true, acquisitionSources: true } } } })` — a parent relation plus child/Acquisition-Source `_count`s only, never the full collections (which the Hierarchy and Acquisition Sources tabs already cover) — no per-row follow-up query. Content is strictly read-only: a `ContextPanel` titled "Location" shows the type (always rendered, since it is a required field), the parent's name (only when present), and the sub-location and Acquisition Source counts (via `_count`, always rendered since zero is itself meaningful administrative context); `VerificationPanel` reuses its existing `readOnly` prop (introduced in Slice 9B.8, unchanged) to omit the composed picker/checkbox while keeping the status badge and Verified-against/Verified-on/Current-version rows (each still hidden when its own data is absent); `TimestampsPanel` renders unchanged (created/updated/verified dates, no ids). The description and access-note fields are deliberately NOT repeated here — both are editable General fields, and duplicating them would add no administrative information this tab exists to show. There is no picker, checkbox, submit button, delete action, image control, hierarchy-mutation control, or Acquisition-Source-mutation control anywhere on this tab, and none of the three panels ever surfaces the record's database id, a foreign key, or a storage path; the Location's own slug is already visible via the existing header subtitle, exactly as on every other tab. General/Hierarchy/Acquisition Sources behavior, Location/Item/AcquisitionSource actions, the Prisma schema, storage, and authorization are all unchanged. The Location reference workspace (General, Hierarchy, Acquisition Sources, Metadata) is now functionally complete, matching the Item, Recipe, Profession, and Category workspaces' shape.

Slice 9G.1 (Admin Dashboard summaries) is complete: `/admin` is a restrained resource-summary workspace, not an analytics dashboard — the old "Manage X" card grid is gone. Five `DashboardSummaryCard`s (Items, Recipes, Professions, Categories, Locations; each a single anchor, never a click handler) show each resource's total record count (zero always renders, never hidden) plus concise supporting context only where it adds value (Items → Acquisition Source count, Recipes → Recipe Ingredient count, Locations → root-location count). A `ContextPanel` titled "Game Version" shows the current version's own name via the existing `getCurrentGameVersion()` (never a new definition, never the version's database id) or a restrained "No current game version selected" status, plus the total Game Version count and a link to the existing `/admin/settings/game-versions` route. A Quick Actions section links directly to each resource's existing `/new` route via a new pure `DASHBOARD_RESOURCE_ROUTES` mapping (`src/lib/admin/dashboard.ts`) — Acquisition Source creation is deliberately excluded, since sources stay Item-owned. All dashboard queries run concurrently via `Promise.all` using `.count()` and the existing `getCurrentGameVersion()` helper only — no full collection loads, no per-row queries, no Prisma schema changes. There are no charts, graphs, percentages, trend indicators, "recent activity" feed, or dashboard mutation form anywhere; `AdminShell` itself was not redesigned.

Slice 9H (final admin visual-consistency pass) is complete: one restrained polish pass across the whole admin surface, not a redesign — no route, server action, Prisma schema, search/query-preservation, validation, image, verification, hierarchy, Acquisition Source ownership, or delete-protection behavior changed. The editor chrome's original purple accent (`--color-admin-accent`/`--color-admin-accent-soft`) is retired: every rule that used it (active tabs, selected record-list rows, a couple of hover states) now uses the same gold `--color-accent`/`--color-accent-soft` the sidebar nav and dashboard already used — one accent identity across the whole admin surface, matching the public site's gold — and the dead tokens were removed from both `globals.css` and `designTokens.colors`. `color-scheme: dark` was added to `:root` so native controls (select arrows, checkboxes, date inputs) render dark instead of clashing with light OS defaults. Several genuinely duplicated inline-style patterns were consolidated into shared classes only after confirming real, repeated duplication (not merely similar-looking code): a shared `.admin-panel-dl` (seven call sites: Timestamps/Verification panels, the dashboard's Game Version panel, all four Metadata tabs), `.admin-image-preview-wrap`/`.admin-image-preview` (the four image-bearing edit pages' identical preview markup), a `.admin-record-empty p { margin: 0; }` rule (replacing ten identical inline styles across all five workspace empty states), a new `.text-accent` utility (matching the existing `.text-muted`/`.text-danger` pair), and `DashboardSummaryCard` rewritten from inline style objects onto shared classes. Inline styles shared with the public site's own established conventions (section spacing, the brand lockup) or that were genuinely single-use and already justified were deliberately left untouched. Workspace layout widths/gaps, tab/panel/table structure, and delete-confirmation hierarchy were audited and found already consistent — a result of every prior 9B–9G slice sharing one component set — so no structural changes were needed there. New tests: a `DashboardSummaryCard` component test and an `admin-visual-consistency.spec.ts` E2E spec proving the gold-not-purple accent, the dark color-scheme, and exactly one h1 across twelve representative routes.

The final Milestone 9 audit — full verification, documentation, commit, and push — is complete: the complete verification matrix ran clean (unit, integration, service, the full E2E suite, lint, type-check, build, diff-check), and this documentation was corrected to state completion accurately. Milestone 9 — Admin Workspace & Game Version Management — is fully complete. Route Hubs are NOT part of Milestone 9 — they were renumbered to a later milestone and stay deferred there. Deployment — previously listed as Milestone 8 — is likewise a separate, later milestone, is not complete, and has not started.

Do not begin Route Hubs, Deployment, contributor tooling, audit logs, user accounts, public-page redesign, mobile redesign, or any later milestone work until explicitly instructed in the milestone conversation.

## AI Workflow Rules

- Do not move ahead without user verification.
- Do not create files unless the current step asks for them.
- Do not install packages unless the current step asks for them.
- Do not refactor unrelated files.
- Do not introduce authentication, database, deployment, or admin features before their milestone.
- Prefer small, reviewable changes.
- Explain commands before running them.
- After changes, always check:
  - `git status`
  - relevant version or test command
- Commit only when the step asks for a commit.

## Technical Defaults

Use the technical brief as source of truth.

Expected stack:

- Next.js
- TypeScript
- Tailwind CSS
- shadcn/ui
- Supabase
- Prisma
- Vercel
- GitHub
- pnpm

## Human Confirmation

Every setup step must be verified before moving to the next one.
